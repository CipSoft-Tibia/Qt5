WebView options:

  -webview2-sdk path .... Set WebView 2 SDK root path
                          [$WEBVIEW2_SDK_ROOT, Setting this enables building of
                          WebView2 plugin for Qt WebView.] (Windows only)
